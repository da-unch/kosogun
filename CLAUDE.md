# CLAUDE.md — kosogun（構想群・額縁研究）作業ガイド

このリポジトリで作業するAI（K・H担当のClaude Code、N担当のClaude）向けの前提とルール。
**サイトの仕様・ファイル構成・使い方・更新履歴は `README.md` にまとめてある。作業前にまず読むこと。**

## このリポジトリについて
「額縁」をテーマにした3人（K / H / N）の共同リサーチ。投稿をWebサイト（GitHub Pages）にタイル状に並べる。
リポジトリの構成やルールは話し合いで今後も変わりうる。

## 絶対に守ること
- **個人名をサイトに出さない**。フォルダ名・URL・表示はすべて頭文字（K / H / N）。テンプレートや設定に氏名を書かない
- 新しく書くドキュメントやコミットメッセージにも氏名は入れず、頭文字で書く（リポジトリは公開）
- 内部資料（`議事録/` `CLAUDE.md` `AI掲示板.md` `README.md`）は `_config.yml` の `exclude` でサイトから外してある。内部向けのファイルを増やしたら `exclude` にも足す
- 機密情報（トークン、OAuthのClient Secret等）はリポジトリにもチャットにも書かない

## 基本の作業フロー
迷ったら「自分のフォルダに置く投稿か？」で判断する。YESならA、サイト全体の見た目・仕組みに関わるならB。

### A. 投稿する（一番よくある作業）
1. `https://da-unch.github.io/kosogun/admin/` を開き「Sign In with GitHub」でログイン
2. 自分の頭文字のコレクションで入力して Publish（`main` に直接コミットされる。ブランチ不要）
3. 並び順は自動で先頭に追加される。変えたいときはCMSの「並び順」ページでドラッグ

### B. サイトの見た目・仕組みを変える（レイアウト・CSS・`_config.yml`・ワークフローなど）
1. ブランチを切る（K・HはClaude Codeで。NはGitHub Connector経由でClaudeに依頼）
2. 変更してそのブランチにpush
3. `main` へのPull Requestを作る
4. 自動でプレビューURL（`/kosogun/pr-preview/pr-<番号>/`）が発行される。誰でもブラウザで確認できる
5. 問題なければMerge。本番に反映されプレビューは自動で片付く

- **構造的な変更で `main` に直接コミットしない**。必ずPR経由
- ドキュメント（`README.md` `CLAUDE.md` `AI掲示板.md`）の更新は `main` 直接でよい
- サイトの仕様を変えたら `README.md` の「現状の仕様」と「更新履歴」も更新する
- どちらの作業でも、始める前に `AI掲示板.md` を確認し、気づいたこと・決めたことは追記する

## 作業上の注意（ハマりどころ）
- URLは `/kosogun/` 配下。テンプレートのリンク・画像は必ず `relative_url` を通す。baseurl はワークフローでビルド時に渡している（`_config.yml` には書かない）
- 本番デプロイは `gh-pages` の既存ファイルを残す設定（PRプレビューのフォルダを消さないため）。サイトから消したいファイル・ページは `gh-pages` からも削除する
- `_data/order.yml` は `sync-order` ワークフロー（`github-actions[bot]`）も書き換える。手で編集するならpush前に最新を取り込む
- 投稿ファイルは `title` があるものだけサイトに出る。フォルダの `README.md` もタイトルを付ければ投稿として出る
- CMSの設定（`admin/config.yml`）を変えたら、開いているCMSは再読み込みしないと反映されない
- フォルダ名 `K/ H/ N/` はCMSのコレクション名・`_config.yml` の `members`・`_data/order.yml` の `type` と一致させる

## 運用ルール
- 各自のフォルダ（`K/` `H/` `N/`）の中身は本人のもの。他人のフォルダの投稿を書き換えるときは本人に確認する
- `議事録/` は共有。話し合いの内容や決まったことを残す
- Nは通常のClaude（ターミナル操作なし）で参加しているため、基本はCMSを使う。構造的な変更はGitHub Connector経由でClaudeにブランチ・PR作成を頼む
- アウトプットの形式や見せ方について前提を勝手に置かない。決まっていないことは「未定」として扱い、議事録や会話で確認する

## 改善アイデアの募集
技術構成・運用フローはこれで完成ではない。もっと簡潔に・使いやすくできるアイデアがあれば、担当に関わらず `AI掲示板.md` に書き込んでよい。実行するかは相談して決める。

## AI掲示板
`AI掲示板.md` は、このリポジトリで作業する複数のAIが直接メモを残し合う場所。
作業を始める前に読み、他の担当者に関わる変更・質問・気づいたことがあれば日付つきで追記する。
過去の発言は書き換えず、追記のみで運用する。
